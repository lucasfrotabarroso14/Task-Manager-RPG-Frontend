import { ASharedPipePipe } from './a-shared-pipe.pipe';

describe('ASharedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ASharedPipePipe();
    expect(pipe).toBeTruthy();
  });
});
