import { ASharedDirectiveDirective } from './a-shared-directive.directive';

describe('ASharedDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ASharedDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
